{
  "name": "cube-streamer-client",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host",
    "test": "node --test tests/config.test.js"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  }
}
